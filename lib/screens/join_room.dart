import 'package:flutter/material.dart';

class JoinRoom extends StatelessWidget {
  static String joinRoomRoute = '/join-room';
  const JoinRoom({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('join room');
  }
}
